/** DI token for the shared Mapper instance. */
export const MAPPER = Symbol.for('@nestjs-automapper/mapper');

/** Metadata key set by `@MapTo`. */
export const MAP_TO = Symbol.for('@nestjs-automapper/map-to');
