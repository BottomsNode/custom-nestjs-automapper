# AD-6 / AD-20: strict resolution is what makes the zero-dependency rule
# on core enforceable rather than aspirational. Nothing may rely on a
# hoisted package it did not declare.
hoist=false
strict-peer-dependencies=false
auto-install-peers=false
resolution-mode=highest
