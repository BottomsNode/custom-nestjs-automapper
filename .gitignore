node_modules/
coverage/
dist/
*.tsbuildinfo
.nx/

# BMAD framework install (tooling, not project content).
# _bmad-output/ IS tracked — it holds the spec, spine, and reviews.
_bmad/

.claude
.agents
skills-lock.json

# Tests stay local — never committed, never published.
*.spec.ts
*.test-d.ts
vitest.config.*
packages/typeorm/src/sql-js.d.ts
_bmad-output/specs/*/spike/
